<Window x:Class="ReqChecker.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="ReqChecker - Desktop Application"
        Height="600"
        Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header/Navigation -->
        <Border Grid.Row="0" Background="#F0F0F0" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Title -->
                <TextBlock Grid.Column="0" Text="ReqChecker" FontSize="20" FontWeight="Bold" Foreground="White" Margin="15,0,10,0"/>

                <!-- Navigation Buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Profiles" Command="{Binding NavigateToProfilesCommand}" Margin="5,0,10,5" Padding="10,5" Background="Transparent" Foreground="White" BorderThickness="0"/>
                    <Button Content="Tests" Command="{Binding NavigateToTestsCommand}" Margin="5,0,10,5" Padding="10,5" Background="Transparent" Foreground="White" BorderThickness="0"/>
                    <Button Content="Results" Command="{Binding NavigateToResultsCommand}" Margin="5,0,10,5" Padding="10,5" Background="Transparent" Foreground="White" BorderThickness="0"/>
                    <Button Content="Diagnostics" Command="{Binding NavigateToDiagnosticsCommand}" Margin="5,0,10,5" Padding="10,5" Background="Transparent" Foreground="White" BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content Frame -->
        <Frame x:Name="ContentFrame" Grid.Row="1" Background="White" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
