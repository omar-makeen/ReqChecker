<Page x:Class="ReqChecker.App.Views.ResultsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="20,10,20,10">
            <TextBlock Text="Results" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="Back to Tests" Command="{Binding NavigateToTestListCommand}" Margin="20,0,0,0" Padding="15,5"/>
            <Button Content="Export Results" Command="{Binding ExportResultsCommand}" Margin="10,0,0,0" Padding="15,5"/>
        </StackPanel>

        <!-- Summary -->
        <Border Grid.Row="1" Background="#F5F5F5" Padding="20" Margin="20,0,20,20" CornerRadius="8">
            <StackPanel>
                <TextBlock Text="Summary" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Info -->
                    <StackPanel Grid.Column="0">
                        <TextBlock FontSize="14" Margin="0,5,0,0">
                            <Run Text="Profile: "/>
                            <Run Text="{Binding Report.ProfileName}" FontWeight="SemiBold"/>
                        </TextBlock>
                        <TextBlock FontSize="14" Margin="0,5,0,0">
                            <Run Text="Started: "/>
                            <Run Text="{Binding Report.StartTime}" FontWeight="SemiBold"/>
                        </TextBlock>
                        <TextBlock FontSize="14" Margin="0,5,0,0">
                            <Run Text="Duration: "/>
                            <Run Text="{Binding Report.Duration}" FontWeight="SemiBold"/>
                        </TextBlock>
                    </StackPanel>

                    <!-- Stats -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Border Background="#4CAF50" Padding="15,10" CornerRadius="8" Margin="5,0">
                            <StackPanel>
                                <TextBlock Text="{Binding Report.Summary.Passed}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Foreground="White"/>
                                <TextBlock Text="Passed" FontSize="12" HorizontalAlignment="Center" Foreground="White"/>
                            </StackPanel>
                        </Border>
                        <Border Background="#DC3545" Padding="15,10" CornerRadius="8" Margin="5,0">
                            <StackPanel>
                                <TextBlock Text="{Binding Report.Summary.Failed}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Foreground="White"/>
                                <TextBlock Text="Failed" FontSize="12" HorizontalAlignment="Center" Foreground="White"/>
                            </StackPanel>
                        </Border>
                        <Border Background="#6B7280" Padding="15,10" CornerRadius="8" Margin="5,0">
                            <StackPanel>
                                <TextBlock Text="{Binding Report.Summary.Skipped}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Foreground="White"/>
                                <TextBlock Text="Skipped" FontSize="12" HorizontalAlignment="Center" Foreground="White"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Test Results List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="20,0,20,20">
            <ItemsControl ItemsSource="{Binding Report.Results}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="4" Margin="0,0,0,10" Padding="15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Test Name -->
                                <TextBlock Grid.Column="0" Text="{Binding DisplayName}" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>

                                <!-- Duration -->
                                <TextBlock Grid.Column="1" Text="{Binding Duration}" FontSize="12" Foreground="#666666" VerticalAlignment="Center" Margin="10,0"/>

                                <!-- Status Badge -->
                                <Border Grid.Column="2" Background="{Binding Status, Converter={StaticResource TestStatusToColorConverter}}" CornerRadius="12" Padding="12,4">
                                    <TextBlock Text="{Binding Status}" FontSize="12" FontWeight="Bold" Foreground="White"/>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
