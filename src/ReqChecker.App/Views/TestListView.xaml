<Page x:Class="ReqChecker.App.Views.TestListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      FocusManager.FocusedElement="{Binding ElementName=RunAllTestsButton}">
    <Grid KeyboardNavigation.TabNavigation="Once" KeyboardNavigation.DirectionalNavigation="Cycle">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="20,10,20,10">
            <TextBlock Text="Test List" FontSize="24" FontWeight="Bold"/>
            <Button x:Name="RunAllTestsButton" Content="_Run All Tests" Command="{Binding RunAllTestsCommand}" Margin="10,0,0,10" Padding="15,5" TabIndex="1"/>
        </StackPanel>

        <!-- Test List -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding CurrentProfile.Tests}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="4" Margin="10" Padding="15" Focusable="True">
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftClick" Command="{Binding DataContext.NavigateToTestConfigCommand, RelativeSource={RelativeSource AncestorType=Page}}" CommandParameter="{Binding}"/>
                            </Border.InputBindings>
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Test Name -->
                                    <TextBlock Grid.Column="0" Text="{Binding DisplayName}" FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap"/>

                                    <!-- Status Badge -->
                                    <Border Grid.Column="1" Background="{Binding Status, Converter={StaticResource TestStatusToColorConverter}}" CornerRadius="12" Padding="8,4">
                                        <TextBlock Text="{Binding Status}" FontSize="12" FontWeight="Bold" Foreground="White"/>
                                    </Border>
                                </Grid>

                                <!-- Test Details -->
                                <TextBlock Text="{Binding Type}" FontSize="12" Foreground="#666666" Margin="0,10,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
