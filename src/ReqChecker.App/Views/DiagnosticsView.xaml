<Page x:Class="ReqChecker.App.Views.DiagnosticsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="20,10,20,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Diagnostics" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
                <Button Content="Open Logs Folder" Command="{Binding OpenLogsFolderCommand}" Margin="20,0,0,0" Padding="15,5"/>
                <Button Content="Copy Details" Command="{Binding CopyDetailsCommand}" IsEnabled="{Binding HasLastRun}" Margin="10,0,0,0" Padding="15,5"/>
            </StackPanel>
            <!-- Status Message -->
            <Border Visibility="{Binding StatusMessage, Converter={StaticResource NullToVisibilityConverter}}"
                    Margin="0,10,0,0" Padding="10,8" CornerRadius="4">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="#E8F5E9"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsStatusError}" Value="True">
                                <Setter Property="Background" Value="#FFEBEE"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Text="{Binding StatusMessage}" FontSize="13">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#2E7D32"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsStatusError}" Value="True">
                                    <Setter Property="Foreground" Value="#C62828"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Border>
        </StackPanel>

        <!-- Last Run Summary -->
        <Border Grid.Row="1" Background="#F5F5F5" Padding="20" Margin="20,0,20,20" CornerRadius="8">
            <StackPanel>
                <TextBlock Text="Last Run Summary" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                <TextBlock Text="{Binding LastRunSummary}" FontSize="13" FontFamily="Consolas,Courier New" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- Machine Info -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="20,0,20,20">
            <StackPanel>
                <Border Background="#E3F2FD" Padding="20" CornerRadius="8" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Machine Information" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding MachineInfoSummary}" FontSize="13" FontFamily="Consolas,Courier New" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- Network Interfaces -->
                <Border Background="#FFF3E0" Padding="20" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Network Interfaces" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                        <ItemsControl ItemsSource="{Binding LastRunReport.MachineInfo.NetworkInterfaces}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="4" Margin="0,0,0,10" Padding="15">
                                        <StackPanel>
                                            <TextBlock FontSize="14" FontWeight="SemiBold" Text="{Binding Name}"/>
                                            <TextBlock FontSize="12" Margin="0,5,0,0" Text="{Binding Description}"/>
                                            <TextBlock FontSize="12" Margin="0,5,0,0">
                                                <Run Text="Status: "/>
                                                <Run Text="{Binding Status}" FontWeight="SemiBold"/>
                                            </TextBlock>
                                            <TextBlock FontSize="12" Margin="0,2,0,0">
                                                <Run Text="MAC: "/>
                                                <Run Text="{Binding MacAddress}" FontWeight="SemiBold"/>
                                            </TextBlock>
                                            <ItemsControl ItemsSource="{Binding IpAddresses}" Margin="0,5,0,0">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock FontSize="12">
                                                            <Run Text="• "/>
                                                            <Run Text="{Binding}"/>
                                                        </TextBlock>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
