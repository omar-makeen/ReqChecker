<Page x:Class="ReqChecker.App.Views.RunProgressView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="20,10,20,10">
            <TextBlock Text="Run Progress" FontSize="24" FontWeight="Bold"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="20,0,0,0" Padding="15,5"/>
        </StackPanel>

        <!-- Progress Display -->
        <StackPanel Grid.Row="1" Margin="40" VerticalAlignment="Top">
            <TextBlock Text="{Binding CurrentTestName}" FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <!-- Progress Indicator -->
            <Border Width="200" Height="200" BorderBrush="#E0E0E0" BorderThickness="2" CornerRadius="100" HorizontalAlignment="Center">
                <Grid>
                    <Ellipse Width="180" Height="180" Stroke="#4CAF50" StrokeThickness="4" StrokeDashArray="5,5"/>
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding CompletedTests}" FontSize="36" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding TotalTests, StringFormat='of {0}'}" FontSize="14" HorizontalAlignment="Center" Foreground="#666666"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Status -->
            <TextBlock Text="{Binding CurrentStatus}" FontSize="16" FontWeight="Bold" Margin="0,20,0,0" HorizontalAlignment="Center"/>

            <!-- Stats -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <StackPanel Orientation="Horizontal" Margin="10,0">
                    <TextBlock Text="{Binding CompletedTests}" FontSize="14" FontWeight="Bold" Foreground="#4CAF50"/>
                    <TextBlock Text=" Passed" FontSize="14" Foreground="#4CAF50"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="10,0">
                    <TextBlock Text="{Binding FailedTests}" FontSize="14" FontWeight="Bold" Foreground="#DC3545"/>
                    <TextBlock Text=" Failed" FontSize="14" Foreground="#DC3545"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="10,0">
                    <TextBlock Text="{Binding SkippedTests}" FontSize="14" FontWeight="Bold" Foreground="#6B7280"/>
                    <TextBlock Text=" Skipped" FontSize="14" Foreground="#6B7280"/>
                </StackPanel>
            </StackPanel>

            <!-- Test Results List -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="300" Margin="0,20,0,0">
                <ItemsControl ItemsSource="{Binding TestResults}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="4" Margin="5" Padding="10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Test Name -->
                                    <TextBlock Grid.Column="0" Text="{Binding DisplayName}" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>

                                    <!-- Status Badge -->
                                    <Border Grid.Column="1" Background="{Binding Status, Converter={StaticResource TestStatusToColorConverter}}" CornerRadius="12" Padding="8,4">
                                        <TextBlock Text="{Binding Status}" FontSize="12" FontWeight="Bold" Foreground="White"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</Page>
