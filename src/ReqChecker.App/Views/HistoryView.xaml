<Page x:Class="ReqChecker.App.Views.HistoryView"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
       xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
       xmlns:converters="clr-namespace:ReqChecker.App.Converters"
       xmlns:vm="clr-namespace:ReqChecker.App.ViewModels"
       mc:Ignorable="d"
       Background="{DynamicResource BackgroundBase}"
       FocusManager.FocusedElement="{Binding ElementName=BackToTestsButton}"
       Loaded="OnPageLoaded">

    <Page.Resources>
        <!-- Staggered entrance animation for history items -->
        <Style x:Key="AnimatedHistoryItem" TargetType="Border">
            <Setter Property="Opacity" Value="0"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <TranslateTransform Y="20"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             From="0" To="1"
                                             Duration="0:0:0.3">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                             From="20" To="0"
                                             Duration="0:0:0.3">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource AnimatedPageHeader}" Margin="0,0,0,24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Gradient Accent Line -->
                <Border Grid.Row="0" Height="4"
                        Background="{DynamicResource AccentGradientHorizontal}"
                        CornerRadius="2"/>

                <!-- Header Content -->
                <Grid Grid.Row="1" Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Icon Container -->
                    <Border Grid.Column="0" Width="48" Height="48"
                            Background="{DynamicResource AccentPrimary}"
                            CornerRadius="12" Margin="0,0,16,0">
                        <ui:SymbolIcon Symbol="History24"
                                       FontSize="24"
                                       Foreground="White"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                    </Border>

                    <!-- Title + Subtitle -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Test History"
                                   FontSize="24" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimary}"/>
                        <TextBlock Text="View past test runs and trends"
                                   Style="{DynamicResource TextBody}"
                                   Foreground="{DynamicResource TextSecondary}"
                                   Margin="0,4,0,0"/>
                    </StackPanel>

                    <!-- Action Buttons -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Style="{StaticResource SecondaryButton}"
                                Command="{Binding ClearAllCommand}"
                                Visibility="{Binding IsHistoryEmpty, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Inverse, FallbackValue=Collapsed}"
                                Margin="0,0,8,0">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="Delete24" FontSize="16" Margin="0,0,8,0"/>
                                <TextBlock Text="Clear All"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="BackToTestsButton"
                                Style="{StaticResource SecondaryButton}"
                                Command="{Binding NavigateToTestListCommand}"
                                TabIndex="1">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="ArrowLeft24" FontSize="16" Margin="0,0,8,0"/>
                                <TextBlock Text="Back to Tests"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- Status Message -->
        <Border Grid.Row="1"
                Visibility="{Binding StatusMessage, Converter={StaticResource NullToVisibilityConverter}}"
                Margin="0,0,0,16"
                Padding="16,12"
                CornerRadius="8">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="#1A10B981"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource StatusPass}"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsStatusError}" Value="True">
                            <Setter Property="Background" Value="#1AEF4444"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource StatusFail}"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <StackPanel Orientation="Horizontal">
                <ui:SymbolIcon FontSize="16" Margin="0,0,8,0">
                    <ui:SymbolIcon.Style>
                        <Style TargetType="ui:SymbolIcon">
                            <Setter Property="Symbol" Value="CheckmarkCircle24"/>
                            <Setter Property="Foreground" Value="{DynamicResource StatusPass}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsStatusError}" Value="True">
                                    <Setter Property="Symbol" Value="ErrorCircle24"/>
                                    <Setter Property="Foreground" Value="{DynamicResource StatusFail}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ui:SymbolIcon.Style>
                </ui:SymbolIcon>
                <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                            <Setter Property="Foreground" Value="{DynamicResource StatusPass}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsStatusError}" Value="True">
                                    <Setter Property="Foreground" Value="{DynamicResource StatusFail}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- Filter Tabs -->
        <Border Grid.Row="2"
                Background="Transparent"
                CornerRadius="8"
                Padding="4"
                Margin="0,0,0,16"
                HorizontalAlignment="Left"
                Visibility="{Binding IsHistoryEmpty, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Inverse, FallbackValue=Collapsed}">
            <ItemsControl ItemsSource="{Binding ProfileNames}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <RadioButton Style="{StaticResource FilterTab}"
                                     Content="{Binding}"
                                     Command="{Binding DataContext.SetFilterCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                     CommandParameter="{Binding}"
                                     Margin="4,0,0,0"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>

        <!-- History List with virtualization -->
        <ListBox Grid.Row="3"
                 ItemsSource="{Binding FilteredHistory}"
                 Background="Transparent"
                 BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling"
                 VirtualizingPanel.ScrollUnit="Pixel"
                 SelectionMode="Single"
                 SelectedItem="{Binding SelectedRun, Mode=TwoWay}"
                 AutomationProperties.Name="History runs list"
                 Visibility="{Binding IsHistoryEmpty, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Inverse, FallbackValue=Collapsed}">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,0,0,12"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisualStyle}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource Card}"
                            Padding="16"
                            Cursor="Hand">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Run Summary -->
                            <StackPanel Grid.Column="0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Profile Name -->
                                    <TextBlock Grid.Column="0"
                                               Text="{Binding ProfileName}"
                                               Style="{DynamicResource TextBody}"
                                               Foreground="{DynamicResource TextPrimary}"
                                               FontWeight="SemiBold"/>

                                     <!-- Pass Rate Badge -->
                                     <Border Grid.Column="1"
                                             Background="{Binding Summary.PassRate, Converter={StaticResource PassRateToBrushConverter}, ConverterParameter=Background}"
                                             CornerRadius="4"
                                             Padding="8,4"
                                             Margin="8,0,0,0">
                                         <TextBlock Text="{Binding Summary.PassRate, StringFormat='Pass Rate: {0:N0}%'}"
                                                    Style="{DynamicResource TextCaption}"
                                                    Foreground="{Binding Summary.PassRate, Converter={StaticResource PassRateToBrushConverter}, ConverterParameter=Foreground}"
                                                    FontWeight="Medium"/>
                                     </Border>
                                </Grid>

                                 <!-- Date and Duration -->
                                 <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                     <ui:SymbolIcon Symbol="CalendarLtr24" FontSize="12" Foreground="{DynamicResource TextTertiary}" Margin="0,1,4,0"/>
                                     <TextBlock Text="{Binding StartTime, Converter={StaticResource FriendlyDateConverter}}"
                                                Style="{DynamicResource TextCaption}"
                                                Foreground="{DynamicResource TextTertiary}"/>
                                     <TextBlock Text=" • "
                                                Style="{DynamicResource TextCaption}"
                                                Foreground="{DynamicResource TextTertiary}"/>
                                     <ui:SymbolIcon Symbol="Timer24" FontSize="12" Foreground="{DynamicResource TextTertiary}" Margin="0,1,4,0"/>
                                     <TextBlock Text="Duration:"
                                                Style="{DynamicResource TextCaption}"
                                                Foreground="{DynamicResource TextTertiary}"/>
                                     <TextBlock Text="{Binding Duration, Converter={StaticResource DurationFormatConverter}}"
                                                Style="{DynamicResource TextCaption}"
                                                Foreground="{DynamicResource TextTertiary}"
                                                Margin="4,0,0,0"/>
                                 </StackPanel>

                                 <!-- Status Indicators -->
                                 <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                     <StackPanel Orientation="Horizontal" Margin="0,0,12,0">
                                         <Ellipse Width="8" Height="8" Fill="{DynamicResource StatusPass}" Margin="0,0,4,0"/>
                                         <TextBlock Text="{Binding Summary.Passed, StringFormat='{}{0} Passed'}"
                                                    Style="{DynamicResource TextCaption}"
                                                    Foreground="{DynamicResource StatusPass}"/>
                                     </StackPanel>
                                     <StackPanel Orientation="Horizontal" Margin="0,0,12,0">
                                         <Ellipse Width="8" Height="8" Fill="{DynamicResource StatusFail}" Margin="0,0,4,0"/>
                                         <TextBlock Text="{Binding Summary.Failed, StringFormat='{}{0} Failed'}"
                                                    Style="{DynamicResource TextCaption}"
                                                    Foreground="{DynamicResource StatusFail}"/>
                                     </StackPanel>
                                     <StackPanel Orientation="Horizontal">
                                         <Ellipse Width="8" Height="8" Fill="{DynamicResource StatusSkip}" Margin="0,0,4,0"/>
                                         <TextBlock Text="{Binding Summary.Skipped, StringFormat='{}{0} Skipped'}"
                                                    Style="{DynamicResource TextCaption}"
                                                    Foreground="{DynamicResource StatusSkip}"/>
                                     </StackPanel>
                                 </StackPanel>
                            </StackPanel>

                            <!-- Delete Button -->
                            <Button Grid.Column="1"
                                    Style="{StaticResource GhostButton}"
                                    Command="{Binding DataContext.DeleteRunCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                    CommandParameter="{Binding}"
                                    ToolTip="Delete this run"
                                    VerticalAlignment="Center"
                                    Margin="8,0,0,0"
                                    Padding="8">
                                <ui:SymbolIcon Symbol="Delete24"
                                               FontSize="16"
                                               Foreground="{DynamicResource TextTertiary}"/>
                            </Button>

                            <!-- View Details Icon -->
                            <ui:SymbolIcon Grid.Column="2"
                                           Symbol="ChevronRight24"
                                           FontSize="20"
                                           Foreground="{DynamicResource TextTertiary}"
                                           VerticalAlignment="Center"
                                           Margin="8,0,0,0"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Empty state when no history -->
        <Grid Grid.Row="3"
              Visibility="{Binding IsHistoryEmpty, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Collapsed}">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <!-- Large History Icon -->
                <Border Width="80" Height="80"
                        Background="{DynamicResource BackgroundSurface}"
                        CornerRadius="40"
                        HorizontalAlignment="Center"
                        Margin="0,0,0,24">
                    <ui:SymbolIcon Symbol="History24"
                                   FontSize="40"
                                   Foreground="{DynamicResource TextTertiary}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                </Border>

                <!-- Heading -->
                <TextBlock Text="No Test History Yet"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimary}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,8"/>

                <!-- Description -->
                <TextBlock Text="Run your first test to start tracking results over time."
                           Style="{DynamicResource TextBody}"
                           Foreground="{DynamicResource TextSecondary}"
                           HorizontalAlignment="Center"
                           TextAlignment="Center"
                           MaxWidth="300"
                           Margin="0,0,0,24"/>

                <!-- Call-to-Action Button -->
                <Button Style="{StaticResource PrimaryButton}"
                        Command="{Binding NavigateToTestListCommand}"
                        HorizontalAlignment="Center"
                        Padding="24,12">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Play24" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="Run Tests"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
