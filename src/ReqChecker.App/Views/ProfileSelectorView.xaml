<Page x:Class="ReqChecker.App.Views.ProfileSelectorView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      FocusManager.FocusedElement="{Binding ElementName=RefreshButton}">
    <Grid KeyboardNavigation.TabNavigation="Once" KeyboardNavigation.DirectionalNavigation="Cycle">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="20,20,20,10">
            <TextBlock Text="Select Profile" FontSize="24" FontWeight="Bold"/>
            <Button x:Name="RefreshButton" Content="_Refresh" Command="{Binding LoadProfilesCommand}" Margin="10,0,0,10" Padding="15,5" TabIndex="1"/>
            <Button Content="_Import Profile" Command="{Binding ImportProfileCommand}" Margin="10,0,0,10" Padding="15,5" TabIndex="2"/>
        </StackPanel>

        <!-- Error Message -->
        <Border Grid.Row="1" Background="#FFEBEE" BorderBrush="#FFC5C5" CornerRadius="4" Padding="15" Margin="20,10,20,10" Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding ErrorMessage}" Foreground="#B71C1C" TextWrapping="Wrap" Margin="0,0,10,0"/>
                <Button Content="_Dismiss" Command="{Binding ClearErrorCommand}" HorizontalAlignment="Right" Margin="10,10,0,0" Padding="10,5" TabIndex="3"/>
            </StackPanel>
        </Border>

        <!-- Profile List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="20,0,20,20">
            <StackPanel>
                <ItemsControl ItemsSource="{Binding Profiles}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="4" Margin="0,10" Padding="15">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Profile Name -->
                                    <TextBlock Grid.Row="0" Text="{Binding Name}" FontSize="18" FontWeight="SemiBold"/>

                                    <!-- Source Badge -->
                                    <Border Grid.Row="1" Background="#0078D4" CornerRadius="12" Padding="8,4" HorizontalAlignment="Right">
                                        <TextBlock Text="{Binding Source}" FontSize="12" FontWeight="Bold" Foreground="White"/>
                                    </Border>

                                    <!-- Profile Details -->
                                    <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,10,0,0">
                                        <TextBlock Text="Tests: " FontSize="12" Foreground="#666666"/>
                                        <TextBlock Text="{Binding Tests.Count}" FontSize="12" FontWeight="SemiBold" Margin="5,0,20,0"/>
                                        <TextBlock Text="Schema: " FontSize="12" Foreground="#666666"/>
                                        <TextBlock Text="{Binding SchemaVersion}" FontSize="12" FontWeight="SemiBold" Margin="5,0,20,0"/>
                                    </StackPanel>

                                    <!-- Select Button -->
                                    <Button Grid.Row="1" Grid.ColumnSpan="2"
                                            Content="_Select"
                                            Command="{Binding DataContext.SelectProfileCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                            CommandParameter="{Binding}"
                                            Margin="10,0,0,0"
                                            Padding="20,8"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            TabIndex="10"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Empty State -->
                <TextBlock Text="No profiles found. Import a profile to get started."
                           FontSize="16"
                           Foreground="#666666"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="{Binding Profiles.Count, Converter={StaticResource CountToVisibilityConverter}}"/>

                <!-- Loading Indicator -->
                <TextBlock Text="Loading profiles..." FontSize="16" Foreground="#666666" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
